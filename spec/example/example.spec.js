describe('Test Example', () => {
  beforeAll(() => {
    // operations to run before all tests
  });
  afterAll(() => {
    // operations to run after all tests
  });
  describe('First Test Example', () => {
    it('should be true', () => {
      const check = true;
      expect(check).toBe(true);
    });
  });
});